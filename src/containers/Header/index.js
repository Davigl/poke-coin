import React from 'react';

import {
  AnimatableWrapper, Wrapper, Title, Text, Button, Card, InfoCard
} from './styles';

const Header = () => (
  <Wrapper>
    <Card>
      <InfoCard />
    </Card>
  </Wrapper>
);

export default Header;
